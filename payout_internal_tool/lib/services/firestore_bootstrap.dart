/// Kept as a no-op stub — NZ trip uses Firestore REST (see NzTripService)
/// because Flutter web's Firestore SDK WebChannel is unreliable on some networks.
void configureFirestoreForWeb() {
  // Intentionally empty.
}
